# htsglang Fork Features

Comparison as of 2026-07-22; upstream SGLang and vLLM main branches. Fork status reflects the
htsglang integration branch (`integration/r2`).

This document lists the features carried by the htsglang fork and records, for each, its
implementation status in the fork and whether an equivalent capability is present in upstream SGLang
or upstream vLLM. The `References` column lists documentation, pull requests, issues, and publications
on the feature's topic — covering upstream implementations, other engines, and related research — with
links and dates.

**Column definitions**
- **Feature** — technical name and a one-sentence description.
- **Fork status** — implementation state in `integration/r2`: `Implemented` (code merged, with
  registered tests and/or a validated boot) or `Work in progress` (present but not yet complete or
  validated). Where only a sub-component is unfinished, it is named.
- **Upstream SGLang?** — `yes` / `partial` / `no` / `n/a`, with a flag, PR, issue, or doc reference where applicable.
- **Upstream vLLM?** — `yes` / `partial` / `no` / `n/a`, with a flag, PR, issue, or doc reference where applicable.
- **References** — documentation, PRs, issues, and publications on the feature's topic (upstream, other
  engines, research), with links and dates.

`partial` indicates that a related or base capability exists upstream while the specific variant
described in the Feature column does not.

| # | Feature | Fork status | Upstream SGLang? | Upstream vLLM? | References |
|---|---|---|---|---|---|
| 1 | **Uneven tensor parallelism** (`--rank-tp-ratio auto`): unequal per-rank attention-head / weight shard sizes within a single tensor-parallel group on one node. | Implemented — validated at TP=3 on 1× RTX 5090 + 2× RTX 3080 (Qwen3.6-27B FP8). Greedy decode is *self-deterministic*: byte-identical run-to-run and cold-vs-warm at a fixed config on the same GPUs. (This is not a claim that LLM inference is deterministic in general — floating-point reduction order makes it path-dependent — nor that output is identical across different hardware; see row 11.) | no — TP path requires head counts divisible by TP size; equal shards per rank. | no — TP path requires equal per-rank sharding. | Upstream symmetric TP: docs.vllm.ai/en/stable/serving/parallelism_scaling. Heterogeneous parallelism (per-stage TP degree): HexGen arxiv.org/abs/2311.11514 (2023) · Hetis dl.acm.org/doi/10.1145/3712285.3759784 (SC'25) · Tangram arxiv.org/pdf/2606.16907 (2026) · Cronus arxiv.org/pdf/2509.17357 (2025) · Tessera arxiv.org/pdf/2604.10180 (2026). |
| 2 | **Uneven decode context parallelism** (`SGLANG_UNEVEN_DCP`, `--rank-kv-ratio`): capacity-weighted KV-cache ownership across ranks during decode. | Implemented — token-split variant validated. | partial — DCP present (issue #12196, roadmap #21788); KV cache split evenly (token index mod world size). | partial — DCP present via `decode_context_parallel_size` / `-dcp`; even split. | SGLang DCP: github.com/sgl-project/sglang/issues/12196 · roadmap /issues/21788. vLLM DCP: github.com/vllm-project/vllm/blob/main/docs/serving/context_parallel_deployment.md. Helix Parallelism arxiv.org/pdf/2507.07120 (2025) · Medha arxiv.org/pdf/2409.17264 (2024) · Context Parallelism for Million-Token Inference arxiv.org/pdf/2411.01783 (2024). |
| 3 | **Explicit rank-to-GPU mapping and rank co-location** (`--rank-gpu-id`, `--rank-gpu-memory-mib`): assigns each rank to a named physical GPU (NVML-resolved), permitting multiple ranks per GPU. | Implemented — co-location path requires NCCL ≥ 2.30 (shipped in the fork's Docker image). | no — placement via `CUDA_VISIBLE_DEVICES` / device ordinals; no per-rank physical-GPU index flag. | no — placement via `CUDA_VISIBLE_DEVICES` / device ordinals; no per-rank physical-GPU mapping flag. | NVIDIA MPS (multi-process per GPU): docs.nvidia.com/deploy/mps. Device selection: CUDA_VISIBLE_DEVICES (docs.nvidia.com/cuda/cuda-c-programming-guide). NVML device identity: docs.nvidia.com/deploy/nvml-api. |
| 4 | **Solo drafter placement** (`--speculative-draft-placement solo`): runs the draft model unsharded on one GPU and broadcasts its output instead of all-reduce. | Implemented — with registered unit tests (solo placement, weight/KV planning, vocab broadcast). | no — EAGLE/EAGLE3 draft runs under the model's TP layout (`--speculative-algorithm`, `--speculative-draft-model-path`); no `speculative-draft-tensor-parallel-size` flag. | yes — `--speculative-draft-tensor-parallel-size 1` runs an unsharded draft with a sharded target. | vLLM speculative decoding: docs.vllm.ai/en/latest/features/speculative_decoding/ · Disaggregated Standalone Draft RFC github.com/vllm-project/vllm/issues/42109 (2026). SGLang speculative decoding: docs.sglang.io/docs/advanced_features/speculative_decoding. EasySpec arxiv.org/pdf/2502.02493 (2025). |
| 5 | **Speculative drafter routing**: two draft algorithms (NEXTN/MTP and DFLASH) resident simultaneously, selected per batch at round boundaries by a bandit controller (accept-tokens/round ÷ round duration, rank-0 decision + TP broadcast), with a context-length gate from the drafter training config. | Work in progress — dual residence, per-batch switch, and the bandit controller are implemented (`cross_algo_worker`, with a registered bandit test); the context-length gate from the drafter training config is not yet implemented. | no — adaptive speculative decoding adjusts `speculative_num_steps` / `num_draft_tokens` for one drafter; no switching between draft algorithms. | no — per-request `k` adaptation, confidence early-exit, disable-by-batch-size; single drafter, no algorithm switching. | SGLang adaptive spec: docs.sglang.io/docs/advanced_features/adaptive_speculative_decoding · roadmap github.com/sgl-project/sglang/issues/23705 · AutoSpec RFC /issues/15319 · Dynamic SPD /issues/9319 · DSpark lmsys.org/blog/2026-07-06-dspark-sglang. vLLM: DynamicProposer /pull/26504 · DSL RFC /issues/36657 · Automate Spec RFC /issues/4565. Research: BanditSpec arxiv.org/abs/2505.15141 (ICML 2025) · OnlineSpec arxiv.org/abs/2603.12617 (ICML 2026) · LongSpec arxiv.org/abs/2502.17421 (2025). |
| 6 | **CUDA graph memory aliasing for speculative branches** (#93/#102): inactive speculative-depth CUDA-graph branches hold no physical VRAM via cuMem tag aliasing. | Implemented — `kv_vmm_backing` / adaptive runtime state. | partial — pre-capturing multiple speculative CUDA graphs is on the roadmap (#23705); physical release of inactive branch allocations via VMM aliasing not documented. | partial — cuMem VMM allocator with tag-based offload exists (Sleep Mode); not applied to speculative CUDA-graph branches. | vLLM Sleep Mode: docs.vllm.ai/en/latest/features/sleep_mode/ · CuMemAllocator docs.vllm.ai/en/latest/api/vllm/device_allocator/cumem/. SGLang multiple spec graphs: github.com/sgl-project/sglang/issues/23705. CUDA Virtual Memory Management API: docs.nvidia.com/cuda/cuda-driver-api. |
| 7 | **MoE expert offloading to host RAM combined with uneven TP and uneven DCP** (GPTQ/AWQ/FP8). | Implemented — validated on a 122B A10B MoE across three mismatched GPUs. | partial — `--cpu-offload-gb` layer-granular weight offload (PR #3675, `offload_group_size`); expert-granular offload is an open feature request (#14233); not combined with uneven TP/DCP. | partial — `--cpu-offload-gb` general weight offload via UVA; not expert-granular; not combined with uneven TP/DCP. | vLLM offload: docs.vllm.ai/en/latest/api/vllm/config/offload/. SGLang offload: github.com/sgl-project/sglang/pull/3675 · expert-granular request /issues/14233. KTransformers: github.com/kvcache-ai/ktransformers. llama.cpp `--override-tensor`: github.com/ggml-org/llama.cpp. MoE-Infinity arxiv.org/html/2401.14361 (2024). |
| 8a | **Bespoke GGUF adapter framework** (#129): a registry (`gguf_registry`) + `GGUFAdapterBase` that add per-model-family GGUF loaders (name maps + llama.cpp inverse weight transforms) on top of the generic GGUF path, and read `config.json` / `tokenizer.json` from sibling files for archs whose GGUF metadata `transformers` cannot parse. | Implemented — registry with two families; unit tests (header, sizing). | no — generic GGUF path cannot load these hybrid/new arches (metadata reader crashes; name map differs). | no — generic GGUF path only. | vLLM GGUF: docs.vllm.ai/en/stable/features/quantization/gguf/. llama.cpp GGUF + gguf-py tensor maps: github.com/ggml-org/llama.cpp · format spec github.com/ggml-org/ggml/blob/master/docs/gguf.md. |
| 8b | **Qwen3.5/3.6 GGUF** (GGUF arch `qwen35` dense hybrid-GDN and `qwen35moe`): inverse transforms (GDN value-head retiling, RMSNorm `+1` un-bake, `out_proj` byte-vs-element un-tile, de-fused GDN `in_proj_qkvz`), plus NEXTN/MTP draft loaded from the same file (`blk.<num_layers>`), including MoE draft with routed experts. | Implemented — dense + MoE + NEXTN/MTP; K-quants Q4_K_M … Q8_0 coherent and greedy-deterministic; validated Q6_K, uneven TP=3 (5090 + 2× 3080). | no — arch unsupported by the generic path. | no — arch unsupported by the generic path. | SGLang/vLLM GGUF docs (above). Qwen3.5 GGUF evals: kaitchup.substack.com/p/more-qwen35-gguf-evals-and-speculative (2026). Speculative decoding from the same file: docs.sglang.io/docs/advanced_features/speculative_decoding. |
| 8c | **Gemma-4 GGUF** (GGUF arch `gemma4`, dense): inverse transforms distinct from Qwen (quantized `token_embd` dequantized to a dense embedding; **identity** norm handling — the Gemma export does not bake `+1` into RMSNorm gammas, the opposite of the Qwen export; tied `lm_head`; `k==v` shard duplication on full-attention layers). | Implemented — Gemma-4-31B-it Q4_K_M validated (TP=1 on RTX 5090, ~61 tok/s, coherent + self-deterministic; uneven TP=3 green). MoE / MTP / vision Gemma-4 GGUF deferred (fail-fast). Only Q4_K_M empirically verified. | no — arch unsupported by the generic path (metadata reader derives a per-layer `num_key_value_heads` list the strict config rejects). | no — arch unsupported by the generic path. | vLLM/llama.cpp GGUF (above). Gemma requires `--attention-backend triton` (rejects flashinfer). |
| 8d | **GGUF K-quant compute kernels** (`sgl-kernel` MMQ/MMVQ): tuned K-quant MMVQ (K-split + column amortization), a per-device MMVQ↔MMQ crossover (MMQ capped to small batch — the stock path used MMQ for K-quants at all batch sizes, penalizing prefill), batched MMVQ (`ncols_dst ≤ 8`), quantized vocab/embedding, Q8_0, and I-Matrix quant via MMVQ. | Implemented — merged from `feat/kquant-kernel`; kernel tests. | partial — generic GGUF MMQ/MMVQ kernels exist; the per-device crossover, prefill-oriented MMQ cap, and quantized-vocab path are fork additions. | partial — generic GGUF MMQ/MMVQ kernels exist; same additions not present. | vLLM GGUF quant kernels: docs.vllm.ai/en/stable/features/quantization/gguf/. llama.cpp MMQ/MMVQ: github.com/ggml-org/llama.cpp. |
| 8e | **Uneven-TP × GGUF correctness** (composes GGUF with feature #1): K-quant 256-element superblock alignment on every per-rank column/row split; GDN `in_proj` head-partition block coarsening; `Qwen2MoeMLP` vec-aligned, quant-symmetric TP units; GGUF-MoE MMQ out-of-bounds expert-id fixes under uneven expert sharding; per-rank local-expert-count guard. Same group-alignment applied to compressed-tensors AWQ/GPTQ INT4. | Implemented — a series of merged bugfixes (#80, #81, #82, #109) with registered tests. | no — uneven TP itself is absent upstream, so these alignment paths do not exist. | no — uneven TP absent upstream. | Depends on feature #1 (uneven TP). llama.cpp K-quant block layout: github.com/ggml-org/ggml/blob/master/docs/gguf.md. |
| 8f | **Multimodal and dynamic-quant GGUF variants**: load a vision tower from a companion `mmproj` GGUF; load unsloth "UD" dynamic-quant GGUFs (dense-F16 `out_proj`, mixed precision). | Implemented — UD Q6_K_XL (+ mmproj vision tower) validated in the benchmark matrix; UD Q8_K_XL infeasible on the reference rig (size + a known Q8 loader limitation). | partial — generic GGUF path does not load these companion-`mmproj` / mixed-precision UD variants for the affected arches. | partial — same. | vLLM/llama.cpp GGUF (above). unsloth dynamic GGUF quants: huggingface.co/unsloth. |
| 9 | **Checkpoint/restore to disk (hibernate)** (#89): persists warm server state to disk so it survives process exit and reloads without full initialization. | Implemented and validated for dense GGUF (load 50 s → 8–14 s under uneven TP=3, survives process exit; `model_loader/hibernate`); the FP8 path is functional with negligible load-time benefit; MoE-model hibernation deferred. | no — offload/wake-up exists for the diffusion server (PR #19152); no full-state disk snapshot for the LLM server. | partial — Sleep Mode releases/restores memory in-process; CUDA checkpoint/restore to persistent snapshot proposed (RFC #34303), not merged. | vLLM Sleep Mode: docs.vllm.ai/en/latest/features/sleep_mode/ · CUDA C/R RFC github.com/vllm-project/vllm/issues/34303. SGLang offload/wake-up: github.com/sgl-project/sglang/pull/19152. ServerlessLLM arxiv.org/pdf/2401.14351 (2024) · Tangram arxiv.org/pdf/2512.01357 (2025) · PipeBoost arxiv.org/pdf/2503.17707 (2025) · CRIU criu.org. |
| 10 | **Measured VRAM budget** (component registry, measured KV-cache remainder, two-boot convergence, `--rank-gpu-memory-mib`): per-rank absolute MiB budget derived from measured component usage rather than a global fraction. | Implemented — per-rank absolute MiB budget plus self-calibrating KV split (boot logs a vector hint fed back on restart). | partial — memory profiling with fraction-based `mem-fraction-static`; per-rank absolute MiB budget not present. | partial — memory profiling with fraction-based `gpu-memory-utilization`; per-rank absolute MiB budget not present. | vLLM memory config: docs.vllm.ai/en/stable/configuration/optimization/ · offload docs.vllm.ai/en/latest/api/vllm/config/offload/. SGLang `mem-fraction-static`: docs.sglang.io (server arguments). NVML memory query (`nvmlDeviceGetMemoryInfo`): docs.nvidia.com/deploy/nvml-api. |
| 11 | **Cross-architecture speculative determinism** (verify synchronization and CUDA-graph padding across sm86 + sm120; sampling broadcast from rank 0). | Implemented — the three divergence root causes resolved; the emitted greedy *token sequence* is reproducible across the mixed-architecture TP group. This is output-preserving reproducibility (spec decode emits the target model's argmax chain, and the accept decision is taken once on rank 0 and broadcast), **not** bit-identical activations — sm86 and sm120 reduce in different order, so the low-order bits differ and are not claimed to match. | partial — deterministic inference mode exists (`--enable-deterministic-inference`, batch-invariant ops); mixed-GPU-architecture TP groups not addressed. | partial — determinism work exists (batch-invariant ops); mixed-GPU-architecture TP groups not addressed. | SGLang deterministic inference: github.com/sgl-project/sglang/blob/main/docs/advanced_features/deterministic_inference.md · issue /issues/10278 · lmsys.org/blog/2025-09-22-sglang-deterministic. Batch-invariant kernels: Thinking Machines (thinkingmachines.ai/blog/defeating-nondeterminism-in-llm-inference/). Batch Speculative Decoding Done Right arxiv.org/pdf/2510.22876 (2025) · Bit-Exact AI Inference Verification arxiv.org/pdf/2606.00279 (2026). |
| 12 | **Weightless KV lane**: a meta-device worker holds only KV cache and attention while the fast head holds the weights. | Implemented — chunked prefill and graph-decode paths in place. | no. | no. | Attention/KV separation from weight compute (disaggregation): Adrenaline arxiv.org/pdf/2503.20552 (2025) · Mooncake usenix.org/system/files/fast25-qin.pdf (FAST'25) · CrossPool arxiv.org/pdf/2606.24506 (2026). |
| 13 | **Rig dashboard / planner UI**: capacity-planning tool reporting work-normalized J/token under uneven DCP. | Implemented — standalone tool (`tools/rig_dashboard`). | n/a — external tooling; SGLang exposes Prometheus metrics. | n/a — external tooling; vLLM exposes Prometheus metrics (docs.vllm.ai/en/latest/design/metrics/). | GPU telemetry: NVIDIA DCGM developer.nvidia.com/dcgm · NVML docs.nvidia.com/deploy/nvml-api · Grafana grafana.com. Engine metrics: vLLM docs.vllm.ai/en/latest/design/metrics/ · SGLang docs.sglang.io (metrics). |
| 14 | **Prefill/decode disaggregation, single-node heterogeneous split** (`local_proxy`, `mooncake_tcp` loopback): prefill runs solo on the fastest card (TP=1, no cross-GPU traffic), decode runs distributed under uneven-TP/DCP on the same node, with GDN/Mamba state handoff. | Implemented — single-node PD pair green (#99 M1/M2), token-vector KV re-scatter, crash-robust. | yes (base) — sglang provides PD-disaggregation + the mooncake transfer stack; the single-node solo-prefill + uneven-TP/DCP decode + GDN-state handoff is the fork delta. | yes (base) — vLLM has P/D disaggregation; single-node heterogeneous solo-prefill split is not its target. | SGLang PD: docs.sglang.io/docs/advanced_features/pd_disaggregation. Mooncake: usenix.org/system/files/fast25-qin.pdf. |
| 15 | **Uneven-TP quantization correctness + upstream quant bugfixes**: GPTQ-MoE `w2_scales` sharding fix at TP>1 (a genuine stock-sglang load defect, even and uneven TP); AWQ marlin MoE zero-point staging fix for `group_size < k-tile` (g=32); uneven-TP `moe_wna16` K-mask illegal-memory-access fix; compressed-tensors and AutoRound-int4 uneven-TP group alignment. | Implemented — bugfixes #83, #85, GPTQ `w2_scales` (even + uneven). | partial — AWQ/GPTQ/compressed-tensors/AutoRound exist upstream, but stock GPTQ-MoE fails to load at TP>1 (fork fixes it) and uneven-TP group alignment is absent. | partial — vLLM has its own Marlin/AWQ/GPTQ; the specific sglang GPTQ-MoE TP>1 defect is sglang-only. | SGLang quantization: docs.sglang.io/docs/advanced_features/quantization. Marlin: github.com/IST-DASLab/marlin. |
| 16 | **Fast-lane priority scheduling** (`--enable-fast-lane`): an opt-in latency-priority class that preempts a tagged request into the running batch, with a reserved-heavy-slots floor + heavy-aging to prevent starvation; default off (default path unchanged). | Implemented — Variant C Stage 0 (`--enable-fast-lane`, `--fast-lane-priority`, `--fast-lane-reserved-heavy-slots`, `--fast-lane-heavy-aging-ms`). | partial — sglang has a priority scheduling subsystem the fork builds on; this reserved-floor fast-lane preemption class is the fork addition. | partial — vLLM has priority scheduling / preemption; not this reserved-floor fast-lane class. | SGLang scheduling / priority: docs.sglang.io (server arguments). |
| 17 | **HiCache (hierarchical KV cache) under uneven-TP/DCP**: makes sglang's tiered KV cache (host-RAM L2 + file L3) correct under non-uniform per-rank layouts — global→owned-compact KV index translation, a storage-prefetch NCCL-deadlock fix under uneven-DCP, and a hybrid-SWA host-pool over-allocation fix. | Implemented — DCP index translation + prefetch-deadlock + host-pool fixes. | yes (base) — HiCache is upstream sglang; correctness under the fork's uneven-TP/DCP layouts is the fork delta. | n/a — vLLM uses LMCache / other KV-offload, not HiCache. | SGLang HiCache: docs.sglang.io/docs/advanced_features/hicache. |
| 18 | **TP > num_kv_heads (replicated-KV + token-sharding)**: lets the tensor-parallel degree exceed the model's KV-head count — and, via co-location, the physical GPU count (validated at TP=5, #62) — by replicating the few KV heads and token-sharding the KV, including GQA re-grouping down to single-head geometries. | Implemented — validated TP=5 on 3 cards via co-location (#62). | partial — standard GQA already replicates KV heads when tp>num_kv_heads, but stock even-TP still requires head-count divisibility; the fork combines replication with uneven-TP + token-sharded DCP + small (gqa=1) geometries. | partial — vLLM replicates KV under GQA similarly; not combined with uneven-TP / token-sharded DCP. | Depends on features #1 / #2. |
| 19 | **Broad model bring-up under uneven-TP, including small models**: Qwen3.6-27B (GDN) and 35B-A3B (MoE) at uneven TP=3; Gemma-4 31B dense (triton per-rank q-head workspace, EAGLE3 speculators-format #101) and 26B-A4B MoE SWA-hybrid (vision-ignore mapper, gated-GeLU Marlin MoE, `--swa-pool-sizing` long-context cap); small / small-head-count (2B, draft) models via replicated-KV GQA handling. | Implemented — per-model; Gemma-4 EAGLE3 head fix (#101), 26B-A4B boot fix, swa-pool-sizing. | n/a — model support; the uneven-TP variants + Gemma-4 speculators/SWA fixes are the fork code. | n/a — model support. | SGLang supported models: docs.sglang.io/supported_models/text_generation/generative_models.html. Gemma: ai.google.dev/gemma. |

## Guarded / descoped (implemented in code, deliberately gated off)

These were built and evaluated in code, then gated off — listed for completeness because real code work went into them, but they are not shipped as usable capabilities.
- **Tree speculative decoding with `--speculative-eagle-topk > 1` under uneven-weighted DCP (#76)** — implemented and GPU-tested; found silently non-greedy under weighted DCP and perf-negative on this PCIe rig; restored as a hard fail-fast guard with a CPU test.
- **SWA-DCP Stage B** — implemented and evaluated (~+6-10%), descoped as not worth the cost; Gemma-4 SWA long-context is served instead by the `--swa-pool-sizing` cap (row 19).

## Scope note

This matrix lists only capabilities with landed code (verified against `integration/r2`). Internal-roadmap items that are planned or only partially prototyped — e.g. cross-vendor HTCCL AMD bring-up, a host-RAM tiered-KV fabric for the weightless lane, downloading VRAM and a draft-KV-pool DCP layout — are intentionally excluded until they land.

## Sources

**Speculative decoding (upstream and research)**
- vLLM: github.com/vllm-project/vllm/pull/26504 · /issues/36657 · /issues/4565 · /issues/42109 · docs.vllm.ai/en/latest/features/speculative_decoding/
- SGLang: github.com/sgl-project/sglang/issues/23705 · /issues/15319 · /issues/9319 · docs.sglang.io/docs/advanced_features/adaptive_speculative_decoding · docs.sglang.io/docs/advanced_features/speculative_decoding · lmsys.org/blog/2026-07-06-dspark-sglang
- Research: arxiv.org/abs/2505.15141 (BanditSpec) · arxiv.org/abs/2603.12617 (OnlineSpec) · arxiv.org/abs/2502.17421 (LongSpec) · arxiv.org/pdf/2502.02493 (EasySpec)

**Heterogeneous parallelism and context parallelism**
- arxiv.org/abs/2311.11514 (HexGen) · dl.acm.org/doi/10.1145/3712285.3759784 (Hetis) · arxiv.org/pdf/2606.16907 (Tangram) · arxiv.org/pdf/2509.17357 (Cronus) · arxiv.org/pdf/2507.07120 (Helix) · arxiv.org/pdf/2409.17264 (Medha) · arxiv.org/pdf/2411.01783 (Context Parallelism)
- docs.vllm.ai/en/stable/serving/parallelism_scaling · github.com/sgl-project/sglang/issues/12196 · github.com/vllm-project/vllm/blob/main/docs/serving/context_parallel_deployment.md

**MoE expert offloading**
- github.com/kvcache-ai/ktransformers · github.com/ggml-org/llama.cpp · arxiv.org/html/2401.14361 (MoE-Infinity) · github.com/sgl-project/sglang/pull/3675 · github.com/sgl-project/sglang/issues/14233 · docs.vllm.ai/en/latest/api/vllm/config/offload/

**KV/attention disaggregation**
- arxiv.org/pdf/2503.20552 (Adrenaline) · usenix.org/system/files/fast25-qin.pdf (Mooncake) · arxiv.org/pdf/2606.24506 (CrossPool)

**Checkpoint/restore and memory**
- arxiv.org/pdf/2401.14351 (ServerlessLLM) · arxiv.org/pdf/2512.01357 (Tangram) · arxiv.org/pdf/2503.17707 (PipeBoost) · github.com/vllm-project/vllm/issues/34303 (CUDA checkpoint/restore) · docs.vllm.ai/en/latest/features/sleep_mode/ · docs.vllm.ai/en/latest/api/vllm/device_allocator/cumem/ · github.com/sgl-project/sglang/pull/19152 · docs.vllm.ai/en/stable/configuration/optimization/

**GGUF**
- docs.vllm.ai/en/stable/features/quantization/gguf/ · github.com/ggml-org/llama.cpp · github.com/ggml-org/ggml/blob/master/docs/gguf.md · kaitchup.substack.com/p/more-qwen35-gguf-evals-and-speculative

**Determinism**
- github.com/sgl-project/sglang/blob/main/docs/advanced_features/deterministic_inference.md · github.com/sgl-project/sglang/issues/10278 · lmsys.org/blog/2025-09-22-sglang-deterministic · arxiv.org/pdf/2510.22876 · arxiv.org/pdf/2606.00279

**Device identity and telemetry**
- docs.nvidia.com/deploy/mps · docs.nvidia.com/deploy/nvml-api · developer.nvidia.com/dcgm · docs.vllm.ai/en/latest/design/metrics/
