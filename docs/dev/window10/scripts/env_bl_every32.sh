# barlink device + the #600 interim mitigation exactly as production carries it.
export SGLANG_BARLINK=1
unset SGLANG_BARLINK_TRANSPORT
export SGLANG_BARLINK_BAR1_ABORT_CHECK_EVERY=32
unset SGLANG_BARLINK_PEER_WATCHDOG
