# WEG 1 (#1068) -- the seam copy is gone, the store carries the prefix

Branch `feat/weg1-store-read`, base `228a66db3209e72caa13dbafd151cfe2ed86a3a6`
(the Boot-2 tree, log `boot_855_968umbauB_228a66db32_0901_210842.log`).
Spec: `/spinning/gpu-arb/WEG1_BUILD_SPEC_0901.md` (sections 0-13 plus the
addenda of 2026-09-02). Evidence tier of everything below: DESK-PROVEN
(hermetic tests, dry-run, synthetic acceptance). Nothing here is BOOT-PROVEN
until the acceptance boot of section 6 has run.

## 1. The design in one paragraph

Both phase host pools are built from ONE absolute upstream knob,
`--hicache-size` (GB), the TP pin ROW-coupled to the PP pool
(`phase_flip_boot.py`, `#1068 TP PIN ROW MISMATCH` raises on divergence);
the mamba anchor pools of both phases from `--hicache-mamba-host-mib` (MiB
per rank, MIN-synced across the group, `synced_from_local=` on the
provenance line). The prefetch budget is the upstream property
`prefetch_capacity_limit = int(fraction x mem_pool_host.size)` read live
(no stored copy, no floor, no symmetrisation); the rate verdict is the
upstream counter form in both roles (`prefetch_tokens_occupied`). A
`rate_limited` verdict is never a decline: the request is marked deferred,
retried every pass in `kv_arrival_seq` order, held from prefill by PP0 only,
and every mark has exactly one named exit (`LANDED`, `DEFER EXPIRED`,
`DEFER RELEASED`, `DEFER DROPPED`, `DEFER CLEARED AT CUTOVER`). At the
cutover the WHOLE run-willing population (retracted residents AND queue
occupants) is re-issued through the one upstream intake site
(`_add_request_to_queue -> _prefetch_kvcache`) under the incoming binding
generation, in arrival order, occupants not stamped as retracts. Every
exit behind the #915 gate has a term and one line. The per-request seam
copy (A) and its restore path are deleted; the HiCache store is the single
carrier. The verdict stays PP0 (#968).

## 2. Bounded loss (the commit sentence of section 4.5, verbatim)

Deleting the per-request seam copy loses at most chunked_prefill_size
(4096, log 175) tokens of recompute per resident and cutover, and only for
residents whose prompt fits ONE chunk and that are re-allocated in the
layout the copy was taken in: for prompts > 4096 restore_seam_state refused
structurally (schedule_batch.py:3278-3279 PREFILL INCOMPLETE, 7 refusals x
3 ranks on boot_855_968umbauB_228a66db32: log 67083-67091, 151125-151143,
153597-153743, 154534-154536, 191117-191133, 191593-191609, 191645-191658),
for layout changes it refused at :3388-3449 (2 x 3 ranks: log
4688/4690/4735, 191591/191602/191607); the measured restore on that boot
was exactly one request (7fb95686, 67 rows, log 151763-151764/151812-151813/
151861-151862). The store path carries a superset of the copy (all 16
layers vs the rank-local stage slice, #706) plus the GDN leaf anchor at
kv_committed_len (mamba_component.py:1057-1119; #969L committed=14922 ==
copy extent 14922, log 154287 vs 191645) and is the single carrier after
this commit, conformant with the one-chunk law (#939).

## 3. Slices as built (commit per slice on this branch)

| slice | commit | what |
|---|---|---|
| 0 | 7ed7181fcc | seam copy A deleted (schedule_batch, phase_flip_runtime, memory_pool, allocator), 9 test files deleted, T0 zombie pin |
| 1 | c4c7bacc86 | sizing: L13 three-flag contract under `--phase-flip-rebind-hicache`, TP pin rows-coupled, anchor MiB MIN-synced, floor refusals, L8 with `spans_at_prompt_max=` |
| 2 | 846c6797b9 (+fixes) | budget: live property, counter-form brake in both roles, ring rebuilt at rebind, reset() joins all three storage threads with termination and ONE `#1068 RESET JOIN` line, class-agnostic on `HybridCacheController` |
| 3 | c801774d1f (+fixes) | re-issue of residents + occupants in arrival order, `#969C` with population/generation, `#1060` presence key with generation, L5/L7, `fence_proceeds`, A12.2 deferral with its lifecycle table |
| 4 | 285e3685b6 (+fix) | every exit behind the #915 gate named (`host_pool_exhausted`, `host_alloc_failed`, `anchor_pool_exhausted`, `vote_negative`, `alloc_failed_post_vote`, `host_pool_truncated` + L2), scheduler exits counted, intake partition, L4 as `logger.error`, rehome + `reread_pages_into` deleted |
| 5 | dd3a133aa2 (+ fix 4b5b4351bb, + fix 2) | launcher sizes from the IN-TREE demand formula (`python/sglang/srt/planner/weg1_host_sizing.py`, no ratio fallback), refuses instead of falling back, probes the target tree for the contract, proves the A11.4 export, dry-run; deadman stale sweep + pgrep proof pinned to the log; acceptance A1-A12 with trapsafe_count, per-re-admitted-rid follower check (A5/A11.5), A12.3 order check, and a can-fail selftest |

## 4. Launcher (slice 5): `/spinning/gpu-arb/boot_855_train0901.sh`

Backup of the pre-slice file: `boot_855_train0901.sh.bak_20260902_174258_pre_weg1_slice5`
(one backup; the slice-5 fix edited the same file in place, no second backup).

The arithmetic is VERSIONED IN THE TREE:
`python/sglang/srt/planner/weg1_host_sizing.py` (operator decision 6 of
2026-09-02: the consumer of S and M is the tree, so the producer lives
beside it). The launcher runs it as
`python -m sglang.srt.planner.weg1_host_sizing` under `PYTHONPATH=$TREE/python`,
so a tree WITHOUT the module cannot size at all. Before composing anything
the launcher PROBES the target tree for both halves of the contract and
refuses by name if either is missing (`#1068 WEG1 TREE CONTRACT MISSING on
TREE=... [sizing module ...] [slice-1 L13 refusal
ServerArgs._refuse_incomplete_phase_flip_hicache_sizing_1068 in
python/sglang/srt/server_args.py] -- not starting serving`, exit 2). The
launcher's default TREE (`/spinning/wt-train-0901`, at d2b78d38d8 on
2026-09-02) carries neither and is therefore refused by construction: the
WEG1 boot path never silently boots a pre-#1068 tree with the old hicache
flags (operator decision 2). The probe is on the files, never on a branch
name (#859).

```
demand_rows = (n_resident + n_queue + 1) x prompt_max
    n_resident = max_running_requests            (config, 8)
    n_queue    = WEG1_QUEUE_DEPTH                 (knob, default = max_running_requests)
    prompt_max = WEG1_PROMPT_MAX_TOKENS           (knob, default 39365 = Boot-2 load class)
S_demand = ceil(demand_rows x cell_pp0 / 1e9)    (cell_pp0: knob WEG1_CELL_PP0_BYTES, else the MAX
                                                  `cell_pp=` over the previous log's HOST-LEDGER
                                                  lines, else 16384)
S_ledger = floor((MemAvailable - 16 GiB - 10 GiB - 27 GiB - 2 x ranks x M_MIB) / 8e9)   [8 = PP 2S + TP pin 6S]
S        = min(S_demand, S_ledger)               rows(S) = S*1e9 // cell_pp0 + 1 (pool_host/base.py:140-147)
M_MIB    = WEG1_MAMBA_HOST_MIB (default 2400, spec section 5 acceptance value; formula 69 x 37.41 = 2582 -> 2600 printed beside it)
```

The host floor is 16 GiB, the value `host_ledger_preflight.sh` (FLOOR_G=16,
"GiB floor") gates the same boot with; the spec's section 5 writes "16 GB",
which is a unit slip (operator decision 1). The `#1068 WEG1 SIZING TERMS`
line prints the floor in both units (`floor=16 GiB (17.18 GB)`) and the
`#1068 WEG1 LEDGER cap=` line prints the resulting cap.

On the Boot-2 configuration with MemAvailable 119 GB: cap = 119.00 - 17.18
- 10.74 - 28.99 = 62.09 GB, anchors 2 x 3 x 2400 MiB = 15.10 GB, kv budget
46.99 GB over 8 x S -> S_ledger = 5 GB (305176 rows, spans_at_prompt_max
7.75); demand 669205 rows (S_demand 11) > ledger -> the A11.2 line
`#1068 HOST POOL DEMAND EXCEEDS LEDGER demand_rows=669205 (n_resident=8
n_queue=8 chain_lag=1 prompt_max=39365) ledger_rows=305176
spans_at_prompt_max=7.75 -> pool sized to the ledger; ...` is printed and
the boot runs with `--hicache-host-role staging --hicache-size 5
--hicache-mamba-host-mib 2400`. (With the spec's 16e9 literal the same box
gave S = 6 / 366211 rows / 9.30 spans; the 1.18 GB the GiB floor costs sits
exactly on that boundary. Spec section 11 R1 names S = 5 as its own fallback
value.) Refusal (exit 2, no fallback): S_ledger < 1, ledger rows <
max_running_requests x chunked_prefill_size, anchor slots < device_slots +
max_running_requests + 1, a non-integer knob, a NON-POSITIVE knob, config
flag or term (fix 2, round-2 finding nb1: `WEG1_QUEUE_DEPTH=-100` had sized
`S_demand=-58 GB` at rc 0 and the launcher would have composed a negative
`--hicache-size`; now every knob `WEG1_QUEUE_DEPTH` / `WEG1_PROMPT_MAX_TOKENS`
/ `WEG1_CELL_PP0_BYTES` / `WEG1_MEMAVAIL_GB` / `WEG1_MAMBA_HOST_MIB` /
`WEG1_PER_SLOT_RANK0_MIB` / `WEG1_DEVICE_SLOTS`, every flag
`--max-running-requests` / `--chunked-prefill-size` / `--ranks` and every
term of `size_host_pools` refuses BY NAME with its value; the queue depth
alone may be 0, a residents-only load someone chose), a sizing that exits 0
but emits no `WEG1_S_GB` / `WEG1_M_MIB` / `WEG1_PROMPT_MAX` machine line.
Every term is printed with its provenance on ONE `#1068 WEG1 SIZING TERMS`
line (A11.1).

A11.4: `WEG1_PROMPT_MAX_TOKENS` is exported for L8 (`phase_flip_boot.py:2609`
reads it, else `n/a`) and the export is PROVEN, not described: the launcher
reads the value back through `env(1)` in a child process (only exported
variables are visible there) and refuses (exit 2, `#1068 WEG1 A11.4 EXPORT
FAILED`) when the read-back differs; the `hicache flags EFFECTIVE` line
quotes the env value, never the shell variable.

`--dry-run` (or `DRY_RUN_ARG=1`) prints the terms, the ledger verdict, the
serving path the flags select (UnifiedRadixCache, `mem_cache/registry.py:191`,
+ HybridCacheController, `hybrid_pool_assembler.py -> hybrid_cache_controller.py:158`,
+ phase flip with HiCache rebind) and the exact argv, and starts nothing,
writes no log, arms nothing, queries no GPU. `--tp-size`/`--pp-size` in the
argv and `--ranks` of the sizing read the SAME `TP_SIZE`/`PP_SIZE` variables
(one source).

Deadman: before arming, deadmen whose argv names port 30030 and another log
are killed by PID (stale sweep, never a broad pkill); then
`GRACE_S=600 boot_deadman.sh <log> 30030` is armed and the proof line
`deadman pgrep proof: N process(es) pid(s) ... whose argv carries THIS
boot's log <log>` is tee'd INTO the boot log, where acceptance A10 greps it;
a proof of `0 process(es)` is an UNWATCHED boot and A10 FAILS by name (fix
2: the selftest arm `deadman-zero` pins that term; round-2 mutant R3
`elif P.deadman < 1` -> `< 0` had survived without it).

L8 unit (fix 2, round-2 finding nb2): the host-free term of the HOST-LEDGER
POST is MemAvailable from `/proc/meminfo` in GB (1e9) and the floor is
printed through the sizing module's one rendering,
`against the 16 GiB (17.18 GB) floor` (`floor_text()`). Until fix 2 the
value was `free -g` column `available`, which procps prints in GiB floored
to an integer (`-g` is `--gibi`; measured on this box: `free -g` 112 against
`free --giga` 121 for one MemAvailable) under a `GB` label.

### `HICACHE_EXTRA` -- the #810 store watermark (added after boot weg1b1 died on it)

Under `--hicache-host-role staging` the FILE backend is the retention tier,
and the tree REFUSES an unbounded one:
`python/sglang/srt/mem_cache/storage/file/lru_file_evictor.py:151` raises
`ValueError("--hicache-host-role staging requires a bounded file backend, and
this one has no watermark: neither `max_size` nor `min_free_space` is set
...")`. No default is chosen in-tree (COORD-strand16b-810-HOST-ROLE.md
section 11), so the number lives in the launcher, as a knob:

| knob | default | scope |
|---|---|---|
| `HICACHE_EXTRA` | `{"max_size":"200G"}` | shared across the 3 writer ranks, i.e. 200 GB on the store filesystem (`/tmp/hicache_855_<tag>`; ~769 GB free on `/` at 2026-09-02) |

Launcher lines, verified with `grep '#810 STORE WATERMARK'
/spinning/gpu-arb/boot_855_train0901.sh` (2026-09-02):

* `:289-297` -- the comment block naming the refusal site and why the default
  is a launcher knob rather than a tree default;
* `:298` -- `HICACHE_EXTRA=${HICACHE_EXTRA:-'{"max_size":"200G"}'}`. The JSON
  must carry NO SPACES: the argv array expands it unquoted, as one word;
* `:299-300` -- a PRE-FLIGHT REFUSAL, not a warning:
  `case "$HICACHE_EXTRA" in *max_size*|*min_free_space*) ;; *) echo "#810
  STORE WATERMARK MISSING in HICACHE_EXTRA=$HICACHE_EXTRA -- the tree refuses
  staging without max_size/min_free_space; not starting serving" >&2; exit
  2;; esac`. An operator who overrides the knob with a watermark-less value
  is stopped by the launcher instead of by a ValueError three minutes into
  model load;
* `:328` -- the header line tee'd into the boot log:
  `#810 STORE WATERMARK: HICACHE_EXTRA=... (knob HICACHE_EXTRA, default
  max_size 200G shared scope; tree refuses staging without one,
  lru_file_evictor.py:151)`, so the value that ran is readable from the log
  alone;
* `:548` -- the flag itself, applied only when the variable is non-empty:
  `${HICACHE_EXTRA:+--hicache-storage-backend-extra-config $HICACHE_EXTRA}`.

Why it is here at all: boot weg1b1 (2026-09-02 21:32:05Z) died on exactly
this refusal, and the failure reads like a hicache sizing problem rather than
a missing launcher argument.

## 5. Acceptance: `/spinning/gpu-arb/accept_weg1_1068.py <boot_log> [--tree TREE]`

Exit 0 = every measured line PASS, 1 = at least one FAIL (each FAIL names
the missing or violating line), 2 = usage or a NAMED refusal
(`#1068 WEG1 ACCEPTANCE REFUSED`). The A10 host-free threshold is
`FLOOR_BYTES` of the tree that booted, read from
`<tree>/python/sglang/srt/planner/weg1_host_sizing.py` and expressed in the
L8 unit (16 GiB = 17.18 GB), never a literal in the script; the tree is
`--tree`, else `$WEG1_TREE`, else the log's own header line
`tree        : <path> @ <sha> (<branch>)` (the launcher writes it), and a
tree without the module refuses by name. `--selftest --tree TREE` runs the
script against seven synthetic logs: one lacking every line (must exit 1,
each FAIL naming its missing line), one consistent two-cutover boot (must
exit 0; its tree is resolved from the synthetic header, the way `main()`
does without `--tree`), two follower-gap logs in which PP0 admits a
re-issued rid with `prefix_lens=4100` and PP2 never materialises it
(cutover 1 only: A5 FAILS naming rid/follower/P while A11.5 still PASSES on
cutover 0 and NAMES cutover 1; both cutovers: A5 and A11.5 FAIL) -- the
can-fail proof per rid --, one whose deadman proof reads `0 process(es)`
(A10 FAIL naming the unwatched boot), and two at the floor boundary (host
free 16.5 GB: A10 FAIL naming `< floor 16 GiB (17.18 GB)`; 17.3 GB: A10
PASS). The selftest also scores the two renderings of the ADMISSION-WEDGE
token the way the log pass does (verdict form genuine=1, description form
genuine=0). Every marker is counted trap-safe (bare next to genuine, #995,
via `devtools/trapsafe_count.py`; the A9 family additionally through the
CLI itself, cross-checked). Every grepped line exists verbatim in the tree
at HEAD (`MARKERS[*].tree_anchor` + site; the tree test greps each).

| line | measures | tree site |
|---|---|---|
| A1 | L8 on all ranks, tp_rows == pp_rows, rows identical across ranks, `#905 HOST-POOL CLEAR size=rows` on 2 pools x ranks, 6 provenance lines with one `host_anchor_slots`, `Allocating` GB == rows x cell per pool, 0 MISMATCH | phase_flip_boot.py:2616/2532/2548, pool_host/base.py:322/180, memory_pool_host.py:465/432 |
| A2 | every `[#719 hicache-rebind] rebound ... to the '<phase>' pools (generation G` followed on its rank by `#915 PREFETCH LIMIT now=... phase=<phase> generation=G`; `now` uniform and == int(fraction x size), role=staging | hicache_phase_binding.py:274, prefetch_budget.py:87 |
| A3 | per rank and cutover: queue after == k+m, dropped 0, count(#969C) == k+m (not rate-limited), L7 present with declined=0; 0 UNREGISTERED, 0 `reason=host_pool_exhausted`, 0 `declined:attempted_but_unregistered` | scheduler.py:5850/5657/5514, phase_flip_runtime.py:10675, unified_radix_cache.py:3626 |
| A4 | TRUNCATED lines: lost <= chunk and over_bound=false; FETCH CAP kv != pool rows and lost <= chunk (deviation 4 below) | unified_radix_cache.py:3654, hicache_storage.py:1925 |
| A5 | PER RE-ADMITTED RID: population = the `#969C READMIT-PREFETCH` rids of every PP0 cutover; P = prefix_lens of the FIRST PP0 `#788 PP-ADMISSION verdict=ADMIT` naming the rid inside the cutover window; for P > 0 EVERY follower (every rank but PP0) must carry `#968 PREFIX-EXEC materialised rid=<rid> ... loaded=P` in that window, else FAIL naming rid, follower and P; rids with P == 0 and rids without a PP0 ADMIT in the window are NOT-MEASURED by name (A5.noprefix / A5.noadmit), never a silent PASS; PASS names the denominator (`N of M re-issued rid(s) with P>0 ...`); plus 0 SHORTFALL, scheduled == loaded, PP1/PP2 congruent | pp_admission_congruence.py:2498/2432, scheduler.py:9828 (rids/prefix_lens are `,`-joined at :9709/:9722), scheduler.py:5657 |
| A6 | `[#939 double-prefill]` VALUE lines within_bound=true, readmitted > 0 somewhere, fence_proceeds=0 | producer_phase_census.py:1162/1165 |
| A7 | first PP0 `#788 PP-ADMISSION verdict=ADMIT` after a `#969L COMMIT-AT-INSERT committed=P>0` names prefix_lens == P (observed sample; #969L is rate-limited) | scheduler.py:9828, unified_radix_cache.py:1362 |
| A8 | 0 W12, 0 DEFER LIMIT, every `#1050 CUTOVER ROW RECLAIM` behind a `[#703 flip-writeback]` fence with outstanding=0 | staging_write_ring.py:147, phase_flip_runtime.py:1658/10891, hicache_flip_writeback.py:187 |
| A9 | 0 `[#783 seam-state]`, 0 `PHASE-FLIP #920`, 0 `#937 STALE PREFETCH INSERT REFUSED`, 0 `#1035 PREFETCH DROPPED` (trapsafe CLI == pass); on the last `[#915 prefetch-gate]` line per rank the declines INTO RECOMPUTE undeferrable + defer_expired + defer_dropped == 0; `defer_refused` is READ and printed, not a FAIL term (deviation 5) | match_refusal_census.py:335/337, scheduler.py:6122-6251, unified_radix_cache.py:1873 |
| A10 | `#905` sizes rank-uniform per clear_epoch, host free after >= the tree's FLOOR_BYTES in GB (16 GiB = 17.18 GB, source named on the report; fix 2), deadman proof >= 1 process (0 = unwatched boot, FAIL by name), oom_kill delta 0 (baseline line vs live counter or `--oom-after`), 0 genuine ADMISSION-WEDGE (verdict form `ADMISSION-WEDGE: N queued, 0 running, and NO first token for Xs`, invariant_checker.py:835 with the constant at :761; the http_server.py:1029 description "`ADMISSION-WEDGE: N queued`" scores bare only), READY line; corridor NOT-MEASURED by name (Phase-4 item) | base.py:322, invariant_checker.py:761/835, http_server.py:1029/3135, launcher |
| A11 | at least one cutover with k+m >= 4; TP `Decode batch phase=tp, #running-req >= 2` within 60 s after a pp_to_tp cutover; TRUNCATED per cutover <= max(0, k+m - floor(spans_at_prompt_max)); tp_to_pp: re-issued rids with committed > 0 get a PP0 ADMIT with prefix_lens > 0; A11.5 FOLLOWER HALF: at least ONE big PP0 cutover where every rid admitted with prefix_lens=P > 0 is materialised on every follower with loaded=P (existential over the big cutovers, A5 is the per-rid universal); cutovers not satisfying it are NAMED in the PASS line | metrics_reporter.py:1294, scheduler.py:9828, pp_admission_congruence.py:2498 |
| A12 | per rank count(DEFERRED) == LANDED + DEFER EXPIRED + DEFER RELEASED + DEFER DROPPED + sum(n over DEFER CLEARED AT CUTOVER); CLEARED line present per cutover; 0 `#915 STAGING RING REBUILD FAILED`; A12.3 ORDER on PP0: the first `#788 ... ADMIT` naming a deferred rid must come AFTER that rid's `#1068 PREFETCH LANDED` line (FAIL otherwise); deferred rids admitted without any LANDED line are named (A12.unlanded), not scored | scheduler.py:6155-6287, hicache_phase_binding.py:797 |

Spec-vs-tree drift followed in the acceptance (named, not silent): the
rebind line quotes the phase (`to the 'tp' pools`); L3 carries `site=`; L4
carries `verdict=`; the #939 line carries a rate-limiter `suppressed=` after
`fence_proceeds=`; `rate_limited` on the gate line is not a decline count
(A12.3/A12.6(2) as built); `#969C` is not rate-limited while `#969L` is;
the `#788` line joins `rids=`/`prefix_lens=` with commas and no spaces
(scheduler.py:9709/9722), so the parser reads the whole field, never the
first rid only.

## 5a. Named deviations from the spec (slice 5, its fix and fix 2)

1. Host floor 16 GiB, not the spec's "16 GB" (section 5): unit slip,
   operator decision 1; the gate `host_ledger_preflight.sh` applies GiB.
   Consequence on the Boot-2 box: S = 5 instead of 6.
2. The provenance line of section 4.6 ("requested/ledger/EFFECTIVE") is
   spelled `demand=/ledger= -> EFFECTIVE=` for S and
   `formula=/knob= -> EFFECTIVE=` for M: "requested" does not say WHO
   requested; the demand (S) and the knob (M) do.
3. `cell_pp0` is the MAX over the previous log's `cell_pp=` fields, not
   "the last HOST-LEDGER line" (section 5): pool_host/base.py:140-147 sizes
   each rank as int(S*1e9 // cell_r) and the MIN sync makes the rank with
   the largest cell binding; the literal last line is PP2's 8192 and would
   size PP0 short. The knob `WEG1_CELL_PP0_BYTES` takes precedence over the
   log.
4. A4 "K == input_len-1 (never 366211)" is not checkable from the
   `#1028B FETCH CAP` line alone (input_len is not on it); built as
   `kv != pool_rows` and `lost <= chunk`.
5. A9: `defer_refused` is read and printed, never a FAIL term. The
   `[#915 prefetch-gate]` counters are boot-cumulative over both phases
   (single emitter unified_radix_cache.py:1873, nothing clears
   PREFETCH_GATE_COUNTS), so a spec-conformant boot with TP-phase
   symmetric-vote declines would otherwise fail; the FAIL terms are the
   declines into recompute (undeferrable, defer_expired, defer_dropped).
6. A11.5 is existential over the big PP0 cutovers (at least one fully
   materialised), with the universal per-rid form living in A5; the
   selftest pins both (gap in one cutover: A5 FAIL + A11.5 PASS naming the
   gap cutover; gap in all: both FAIL).
7. The "ratio zombie" claim, measured (re-counted for fix 2 with
   `git grep` at HEAD): launcher 0 hits for `HICACHE_RATIO`, `_HR_FALLBACK`,
   `--hicache-ratio`, `devtools/weg1_host_sizing`; python/ 1 hit for
   `HICACHE_RATIO` (the "WHAT THIS REPLACES" docstring sentence of
   `weg1_host_sizing.py:20`, prose naming what was deleted, no code path;
   the slice-5 fix commit said 0, that was off by one) and 0 for
   `_HR_FALLBACK`. The token `--hicache-ratio` itself still has 32 hits
   under python/ (the upstream flag definition, comments,
   boot_matrix/arms.py:677): slice 5 deletes only the LAUNCHER emission,
   the tree's L13 refusal is what makes the flag unusable on this form.
8. The synthetic selftest log carries S=6 / 366211 rows / 9.30 spans in its
   header lines: a synthetic boot, not the formula's output; the acceptance
   never recomputes the sizing from those lines.
9. Red-first, honestly: on the parent 285e3685b6 the slice-5 test file is
   8 red / 5 green (13 tests). Red: the five sizing tests (the in-tree
   module is absent, ImportError named) and the three dry-run tests that
   expect the sized argv or a SIZING refusal (the launcher's contract probe
   refuses that tree first). Green by construction, named: three launcher
   pins that use fake trees or read the launcher text
   (test_a_sizing_without_machine_lines_stops_the_launcher,
   test_a_tree_without_the_contract_is_refused_by_name,
   test_the_export_is_read_back_through_env_not_echoed) and two pins of the
   operator acceptance artefact
   (test_selftest_can_fail_per_rid_and_per_wedge_form,
   test_every_grepped_line_exists_verbatim_in_the_tree).
10. The corridor (819-1229 MiB NVML-free per card under load) is
    NOT-MEASURED by the acceptance script by name (A10.corridor); the
    reader is a Phase-4 item (operator decision 4).
11. (Slice-5 fix commit body, item 11, carried here so the two lists
    match.) The truncated non-blocking note on the deadman pgrep proof
    could not be read from the round-1 briefing (cut at `pgrep -f "b`); the
    pin stands as built (`pgrep -f "boot_deadman.sh ${LOG}"`, tee'd into
    the log). Round 2 read the full note: the log path is used as a regex
    (dots are wildcards; harmless on these paths, pgrep has no `-F`), and
    the stale sweep runs BEFORE the arming so it cannot hit the new deadman.
    Left as built, by name.
12. Fix 2 (nb1): the queue depth (`n_queue` / `WEG1_QUEUE_DEPTH`) is
    validated as >= 0, not > 0 like every other term: a residents-only load
    (no queue) is a configuration someone chose by name, the demand
    `(n_resident + 0 + chain_lag) x prompt_max` is well defined, and the
    slice-5 test pins it (`n_queue=0` -> 354285 rows). The nb1 defect class
    is the sign, not the zero. Everything else, including the module
    constant `chain_lag`, must be a positive integer (floats: finite and
    > 0).
13. Fix 2 (nb2): the L8 host-free term changed its INSTRUMENT, not only its
    text: `free -g` column `available` is GiB floored to an integer (procps
    `-g` = `--gibi`, measured: `free -g` 112 vs `free --giga` 121 for one
    MemAvailable) and was labelled GB, so "expressed in the L8 unit" was
    not satisfiable on the old value; the emitter now reads MemAvailable
    from `/proc/meminfo` (the sizing module's own instrument) and prints GB
    with two decimals. The acceptance threshold is the tree module's
    `FLOOR_BYTES / 1e9` = 17.18 in that unit, as ordered; the boundary pins
    (16.5 GB FAIL, 17.3 GB PASS) hold on the real emitter's unit.
14. Fix 2 (nb2): the acceptance needs the tree that booted to read the
    floor (`--tree`, `$WEG1_TREE`, or the log header the launcher writes);
    a log without a header and no `--tree` is a NAMED refusal (exit 2), the
    in-tree test passes `--tree` to the selftest. The spec's usage line
    (`accept_weg1_1068.py <boot_log>`) gains the optional argument.

## 6. Open on metal (not desk-decidable)

R1 host RAM at 58 GB pinned against the unattributed #1038 peak (oom_kill
baseline vs after is the instrument); R2 the staging role has never booted
(W12 line, A8); R4 anchor slots (64) vs host-side anchor eviction; N5 the
store read rate under 9 parallel 39k spans (TTFT hole up to ~41 s, no
wedge); the corridor (819-1229 MiB NVML-free per card) from the memts CSV.
The acceptance boot is the section-10 load (4-5 residents of the 39k class
plus queue, >= 3 cutover cycles) with the deadman armed and its pgrep proof
in the log.

## 7. Boot 3 (weg1b3) findings

Log: `/spinning/evidence-665-f1/boot_855_weg1b3_6980c75eac_0902_234752.log`
(51 MB, 200,766 lines; all line numbers are `grep -n` numbers). Tree
`/spinning/wt-weg1 @ 6980c75eac` (feat/weg1-store-read). Full postmortem, with
the four refuter lenses and every claim's evidence tier:
`/spinning/gpu-arb/HANDOVER_WEG1B3_0903.md`. The boot reached READY at
23:51:47Z, flipped five times, served ZERO of the six load-driver requests, and
was ended at 00:06:52 by sglang's own watchdog/SIGQUIT chain. No traceback, no
OOM: 0 `Traceback`, 0 `RuntimeError:`, cgroup `oom_kill` delta 0. Four roots,
one task each.

**R1 -- the store READ serves nothing at the cutover (mission core, task
#1157).** `#1028B FETCH CAP` fires 15 times and carries `claimed=0` on 14 of
those 15 lines: the prefix storage said it had was never handed back, so every
downstream reuse term is vacuous. `acceptance_631_run.py --check` reports
`by_source={'backup_host': 779189, 'unknown': 73651}` with no `prefetch` arm and
`ordering: late=0 never_yet=1915` -- the pages did not arrive late, they did not
arrive. 43,892 `Failed to fetch ... .draft-<hash> from HiCacheFile storage`
lines (PP0 10,280 / PP1 23,460 / PP2 10,152) are the draft model's own store
misses beside it. This is the feature the boot existed to prove, and it did not
execute.

**R2 -- rank-divergent TP admission killed the boot (task #1158).** At 23:59:54,
after the fifth flip reconciled, PP0 and PP2 entered the post-cutover warmup
(`#1033c CUTOVER FORWARD WARMUP begin ... tp layout (1/8)`, log lines 104857 and
104468) and PP1 did not. No `WARMUP done` and no `build window CLOSE` follows on
any rank. The py-spy dumps taken at 00:04:46 show the shape exactly: PP0 and PP2
inside `gdn_triton.extend` waiting for a collective, PP1 idle in
`broadcast_pyobj` at the top of `event_loop_normal` waiting for rank 0. Two
ranks took a state-changing decision and one did not, which under
`raenge-nie-uneins-crash-stop` must be a group STOP at the single site that owns
the decision, not a per-rank compensation. Both watchdogs that tripped (00:06:47,
PP0 and PP2) are the two ranks that were in the forward; PP1 never tripped
because nothing of its own was frozen.

**R3 -- amplifiers (task #1159).** Three mechanisms made the failure window
longer and louder than it had to be. (a) The flip image ran file-backed with
`SGLANG_PHASE_FLIP_IMAGE_TWO_FILE` unset, so `d2h-call` was 56.3-61.0 s of every
68.8-74.4 s leg (82-85 %), and five flips spent 360.0 s of the boot's 1197 s
span; the two-file arm was measured at 4.7-11.2 s per leg on 2026-09-01
(`ACCEPTANCE_train0901_notes.txt`). (b) The #833 STALLED arm fired at line 100502
against rid 679e4568, whose ADMIT prefix was strictly advancing 4096 -> 20480 one
chunk every ~3 s: a chunked prefill in progress read as a frozen decode bundle,
and the 70 s cutover it armed retracted the very request it was building. (c)
The `#1073 abort-word poll RESUMING` line wrote 82,350 of the log's 200,766
lines (41.0 %) at ~97 lines/s/rank, because a persistent condition was reported
at the watchdog's tick rate.

**R4 -- instrument defects (task #1159).** Four instruments reported something
other than what happened. The #939 census was blind on `pp_to_tp` waves: the
seam stamp its writer gates on was spent before `prepare_for_extend` ran, so the
boot's two worst double prefills (679e4568 lost 84027 tokens at 23:56:18,
8f31846b lost 13225 at 23:59:54) were never scored while six census lines exist
for the one `tp_to_pp` wave. `acceptance_631_producer_phase.py` half A printed
PASS on rid 0e880eaf, a first admission in the PP phase reusing a sibling's
system prefix computed in that same phase -- neither a re-admission nor TP decode
on PP-produced context. `boot_deadman.sh` counted `flip_abandoned=1
arm_unfunded=1` from the #1033 health-check prose line that quotes both marker
names; the genuine count of each is 0. `accept_weg1_1068.py` A5 printed PASS
beside "0 of 5 re-issued rid(s) with P>0 materialised", and its corridor row
printed NOT-MEASURED while `corridor_weg1b3.tsv` sat beside the log with card 1
at 180 MiB.
