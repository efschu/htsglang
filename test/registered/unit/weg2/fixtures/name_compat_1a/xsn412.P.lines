[2026-09-24 08:35:37 PP1] WEG2-SLEEP graph tag NOT armed: no_group -- SGLANG_WEG2_GROUP is empty or unset on a rank that carries other SGLANG_WEG2_* variables -- launcher.build_env publishes it at every launch site, so it was lost between the launcher and this process. occurrence=1 for this reason in this process (the line is rate-limited to occurrences 1,2,4,8,...; the count is the denominator, the line is not). The sleeping rank keeps the CUDA-graph capture pool and the flashinfer FLOAT workspace resident (pre-item behaviour, not a silent success)
[2026-09-24 08:35:37 PP2] WEG2-SLEEP graph tag NOT armed: no_group -- SGLANG_WEG2_GROUP is empty or unset on a rank that carries other SGLANG_WEG2_* variables -- launcher.build_env publishes it at every launch site, so it was lost between the launcher and this process. occurrence=1 for this reason in this process (the line is rate-limited to occurrences 1,2,4,8,...; the count is the denominator, the line is not). The sleeping rank keeps the CUDA-graph capture pool and the flashinfer FLOAT workspace resident (pre-item behaviour, not a silent success)
